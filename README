Инструкция как развернуть проект:

1. Клонировать репозиторий командой git clone.
2. Поднянуть необходимые зависимости:
  - для frontend: в терминале перейти в папку frontend - ввести команду npm install
  - для backend: в терминале перейти в папку backend - ввести команду npm install
3. Для запуска проекта после установки зависимостей:
  - для frontend: в терминале в папке frontend - ввести команду npm run dev
  - для backend: в терминале в папке backend - ввести команду npm run develop